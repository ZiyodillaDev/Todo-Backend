class Todos {
  constructor(id, date, text, title, isCompleted) {
    this.id = id;
    this.text = text;
    this.title = title;
    this.date = date;
    this.isCompleted = isCompleted;
  }
}

// Exported
module.exports = Todos;



// Todo iscompleted (shunaqa tekshirilishi kere)
// Process success failure

